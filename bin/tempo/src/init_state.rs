//! Initialize state from a binary dump file.
//!
//! This command loads TIP20 storage slots from a binary file and applies them
//! to the genesis state. The binary format is produced by `tempo-xtask generate-state-bloat`.

use std::{
    cmp::Reverse,
    collections::{BinaryHeap, HashSet},
    fs::File,
    io::{BufReader, Read},
    path::PathBuf,
    sync::{Arc, mpsc},
    thread,
    time::{Duration, Instant},
};

use alloy_primitives::{
    B256, U256, keccak256,
    map::{AddressMap, B256Map, Entry},
};
use alloy_rlp::{BufMut, Encodable};
use clap::Parser;
use eyre::{Context as _, ensure};
use reth_chainspec::EthereumHardforks;
use reth_cli_commands::common::{AccessRights, CliNodeTypes, EnvironmentArgs};
use reth_db_api::{
    cursor::{DbCursorRO, DbDupCursorRW},
    models::{CompactU256, StorageBeforeTx, storage_sharded_key::StorageShardedKey},
    table::Decompress,
    tables,
    transaction::{DbTx, DbTxMut},
};
use reth_ethereum::{chainspec::EthChainSpec, tasks::Runtime};
use reth_etl::Collector;
use reth_primitives_traits::{Account, StorageEntry};
use reth_provider::{
    BlockNumReader, ChainSpecProvider, ChangeSetReader, DBProvider, DatabaseProviderFactory,
    DatabaseProviderROFactory, HashingWriter, PruneCheckpointReader, RocksDBProviderFactory,
    StageCheckpointReader, StaticFileProviderFactory, StaticFileSegment, StorageChangeSetReader,
    StorageSettingsCache, TrieWriter,
    providers::{OverlayBuilder, OverlayStateProviderFactory},
};
use reth_tasks::{RuntimeBuilder, RuntimeConfig, TokioConfig};
use reth_trie::{
    HashBuilder, IntermediateStateRootState, Nibbles, StateRootProgress, StorageRoot,
    TRIE_ACCOUNT_RLP_MAX_SIZE, TrieType,
    hashed_cursor::HashedCursorFactory,
    metrics::TrieRootMetrics,
    node_iter::{TrieElement, TrieNodeIter},
    prefix_set::{TriePrefixSets, TriePrefixSetsMut},
    trie_cursor::TrieCursorFactory,
    updates::TrieUpdates,
    walker::TrieWalker,
};
use reth_trie_db::DatabaseStateRoot;
use reth_trie_parallel::StorageRootTargets;
use tempo_chainspec::spec::TempoChainSpecParser;
use tempo_primitives::TempoPrimitives;
use tracing::{debug, info};

/// Magic bytes for the state bloat binary format (8 bytes)
const MAGIC: &[u8; 8] = b"TEMPOSB\x00";

/// Expected format version
const VERSION: u16 = 1;

/// ETL collector file size (200 MiB per temp file before spilling a new one).
const ETL_FILE_SIZE: usize = 200 * 1024 * 1024;

/// Maximum number of binary dump storage entries to send per worker batch.
const COLLECTION_CHUNK_SIZE: usize = 8192;

/// Bounded channel depth for each collection worker.
const COLLECTION_WORKER_QUEUE_DEPTH: usize = 32;

/// Number of unique storage-history keys to buffer per worker before sending.
const STORAGE_HISTORY_CHUNK_SIZE: usize = 8192;

/// Bounded channel depth for each storage-history writer.
const STORAGE_HISTORY_WORKER_QUEUE_DEPTH: usize = 64;

type DumpEntry = [u8; 64];
type StorageCollector = Collector<Vec<u8>, CompactU256>;
type StorageHistoryEntry = (alloy_primitives::Address, B256);
type StorageHistoryChunk = Vec<StorageHistoryEntry>;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
enum WorkerSetting {
    Auto,
    Count(usize),
}

impl WorkerSetting {
    fn resolve(self) -> usize {
        match self {
            Self::Auto => thread::available_parallelism()
                .map(|cpus| cpus.get().saturating_sub(1).max(1))
                .unwrap_or(1),
            Self::Count(workers) => workers.max(1),
        }
    }
}

fn parse_workers(value: &str) -> Result<WorkerSetting, String> {
    if value.eq_ignore_ascii_case("auto") {
        return Ok(WorkerSetting::Auto);
    }

    let workers = value
        .parse::<usize>()
        .map_err(|_| "workers must be a positive integer or 'auto'".to_string())?;
    if workers == 0 {
        return Err("workers must be at least 1".to_string());
    }
    Ok(WorkerSetting::Count(workers))
}

/// Initialize state from a binary dump file.
#[derive(Debug, Parser)]
pub struct InitFromBinaryDump<C: reth_cli::chainspec::ChainSpecParser = TempoChainSpecParser> {
    #[command(flatten)]
    env: EnvironmentArgs<C>,

    /// Path to the binary state dump file.
    ///
    /// The file should be generated by `tempo-xtask generate-state-bloat`.
    #[arg(value_name = "BINARY_DUMP_FILE")]
    state: PathBuf,

    /// Number of workers to use across collection, storage-history, and trie-root phases.
    ///
    /// Use `auto` to resolve to one less than the available CPU count, with a minimum of 1.
    /// A value of 1 preserves the historical single-worker collection, single-writer
    /// storage-history, and serial trie behavior.
    #[arg(
        long,
        default_value = "auto",
        value_parser = parse_workers,
        value_name = "N|auto"
    )]
    workers: WorkerSetting,
}

impl<C: reth_cli::chainspec::ChainSpecParser<ChainSpec: EthChainSpec + EthereumHardforks>>
    InitFromBinaryDump<C>
{
    /// Execute the init-from-binary-dump command.
    pub(crate) async fn execute<N>(self, runtime: Runtime) -> eyre::Result<()>
    where
        N: CliNodeTypes<ChainSpec = C::ChainSpec>,
    {
        info!(target: "tempo::cli", "Tempo init-from-binary-dump starting");

        let workers = self.workers.resolve();
        info!(target: "tempo::cli", workers, "Resolved init-from-binary-dump workers");

        let etl_dir = self
            .env
            .datadir
            .clone()
            .resolve_datadir(self.env.chain.chain())
            .data_dir()
            .join("etl");
        let environment = self.env.init::<N>(AccessRights::RW, runtime)?;
        let provider_factory = environment.provider_factory;

        let provider_rw = provider_factory.database_provider_rw()?;

        // Verify we're at genesis (block 0)
        let last_block = provider_rw.last_block_number()?;
        ensure!(
            last_block == 0,
            "init-from-binary-dump must be run on a freshly initialized database at block 0, \
             but found block {last_block}"
        );

        info!(target: "tempo::cli", path = %self.state.display(), "Loading binary state dump");

        let file = File::open(&self.state)
            .wrap_err_with(|| format!("failed to open {}", self.state.display()))?;
        let mut reader = BufReader::with_capacity(64 * 1024 * 1024, file);

        let total_entries: u64;
        let mut total_blocks = 0u64;

        // Track addresses and their account data for hashing
        let mut accounts_seen: AddressMap<Account> = AddressMap::default();
        let mut genesis_storage_keys = HashSet::new();

        let mut genesis_storage_changeset_collector: StorageCollector =
            Collector::new(ETL_FILE_SIZE, Some(etl_dir.clone()));

        for (index, entry) in provider_rw.storage_changeset(0)? {
            let raw_key = raw_storage_key(index.address(), entry.key);
            genesis_storage_keys.insert(raw_key.clone());
            genesis_storage_changeset_collector
                .insert(raw_key, CompactU256::from(entry.value))
                .wrap_err("storage changeset ETL insert of genesis storage failed")?;
        }

        let genesis_storage_keys = Arc::new(genesis_storage_keys);
        let mut collection_pool =
            CollectionWorkerPool::new(workers, etl_dir.clone(), genesis_storage_keys)?;
        let mut collection_chunk = CollectionChunk::new(alloy_primitives::Address::ZERO);

        // Process blocks from binary file
        loop {
            // Read next block header; EOF means no more blocks.
            let mut header_buf = [0u8; 40];
            match reader.read_exact(&mut header_buf) {
                Ok(()) => {}
                Err(e) if e.kind() == std::io::ErrorKind::UnexpectedEof => break,
                Err(e) => return Err(e).wrap_err("failed to read block header"),
            }

            // Validate magic
            ensure!(
                &header_buf[..8] == MAGIC,
                "invalid magic bytes in block header"
            );

            // Validate version
            let version = u16::from_be_bytes([header_buf[8], header_buf[9]]);
            ensure!(
                version == VERSION,
                "unsupported binary format version {version}, expected {VERSION}"
            );

            // Skip flags (2 bytes at offset 10)

            // Read address (20 bytes at offset 12)
            let mut address_bytes = [0u8; 20];
            address_bytes.copy_from_slice(&header_buf[12..32]);
            let address = alloy_primitives::Address::from(address_bytes);

            // Read pair count (8 bytes at offset 32)
            let pair_count = u64::from_be_bytes(header_buf[32..40].try_into().unwrap());

            info!(
                target: "tempo::cli",
                %address,
                pair_count,
                "Processing token storage block"
            );

            // Ensure the account exists in the canonical state table (only on first encounter).
            // The binary dump is chunked: generate-state-bloat writes one block per token
            // per chunk, so the same token address appears in multiple blocks. This entry
            // is Vacant on the first chunk and Occupied on subsequent ones.
            // Preserving the genesis account is critical: TIP20 tokens have bytecode (0xEF)
            // set during genesis, and overwriting with Account::default() would clear the
            // code hash, making the token appear uninitialized.
            if let Entry::Vacant(e) = accounts_seen.entry(address) {
                let hashed_address = keccak256(address);
                let mut account_cursor = provider_rw
                    .tx_ref()
                    .cursor_read::<tables::HashedAccounts>()?;
                let account = match account_cursor.seek_exact(hashed_address)? {
                    Some((_, account)) => account,
                    None => {
                        return Err(eyre::eyre!(
                            "state bloat references account {address} that is missing from genesis"
                        ));
                    }
                };
                e.insert(account);
            }

            if !collection_chunk.entries.is_empty() && collection_chunk.address != address {
                let chunk = std::mem::replace(&mut collection_chunk, CollectionChunk::new(address));
                collection_pool.send(chunk)?;
            }
            collection_chunk.address = address;

            // Read entries into collection-worker batches.
            let mut entry_buf = [0u8; 64];
            let start = Instant::now();
            let mut last_log = start;

            for i in 0..pair_count {
                reader
                    .read_exact(&mut entry_buf)
                    .wrap_err("failed to read storage entry")?;

                collection_chunk.entries.push(entry_buf);
                if collection_chunk.entries.len() >= COLLECTION_CHUNK_SIZE {
                    let chunk =
                        std::mem::replace(&mut collection_chunk, CollectionChunk::new(address));
                    collection_pool.send(chunk)?;
                }

                log_collection_progress(&address, i, pair_count, start, &mut last_log);
            }

            total_blocks += 1;
        }

        if !collection_chunk.entries.is_empty() {
            collection_pool.send(collection_chunk)?;
        }
        let collection_results = collection_pool.finish()?;
        let active_collection_workers = collection_results
            .iter()
            .filter(|result| result.entries > 0)
            .count();
        let collection_worker_chunks: usize =
            collection_results.iter().map(|result| result.chunks).sum();
        let collection_worker_pairs: usize =
            collection_results.iter().map(|result| result.pairs).sum();
        let collection_worker_entries: usize =
            collection_results.iter().map(|result| result.entries).sum();
        total_entries = collection_worker_entries as u64;

        let mut hashed_collectors = Vec::with_capacity(collection_results.len() + 1);
        let mut storage_changeset_collectors = Vec::with_capacity(collection_results.len() + 1);
        let mut storage_history_collectors = Vec::with_capacity(collection_results.len());

        for result in collection_results {
            hashed_collectors.push(result.hashed_collector);
            storage_changeset_collectors.push(result.storage_changeset_collector);
            storage_history_collectors.push(result.storage_history_collector);
        }
        storage_changeset_collectors.push(genesis_storage_changeset_collector);

        info!(
            target: "tempo::cli",
            workers,
            active_collection_workers,
            collection_worker_chunks,
            collection_worker_pairs,
            collection_worker_entries,
            "Collection workers finished"
        );

        info!(
            target: "tempo::cli",
            total_blocks,
            total_entries,
            "Entries collected, merging genesis hashed storage into ETL collector..."
        );

        // Merge existing genesis hashed storage into the collector.
        {
            let mut genesis_hashed_collector: StorageCollector =
                Collector::new(ETL_FILE_SIZE, Some(etl_dir));
            let tx = provider_rw.tx_ref();
            let mut cursor = tx.cursor_read::<tables::HashedStorages>()?;
            let mut genesis_count = 0usize;
            let walker = cursor.walk(None)?;
            for row in walker {
                let (hashed_address, entry) = row?;
                let mut key = Vec::with_capacity(64);
                key.extend_from_slice(hashed_address.as_slice());
                key.extend_from_slice(entry.key.as_slice());
                genesis_hashed_collector
                    .insert(key, CompactU256::from(entry.value))
                    .wrap_err("ETL insert of genesis hashed storage failed")?;
                genesis_count += 1;
            }
            hashed_collectors.push(genesis_hashed_collector);
            info!(
                target: "tempo::cli",
                genesis_count,
                "Genesis hashed storage entries merged into collector"
            );
        }

        let storage_settings = provider_rw.cached_storage_settings();
        ensure!(
            storage_settings.storage_v2,
            "init-from-binary-dump only supports storage v2 databases"
        );

        let storage_changeset_factory = provider_factory.clone();
        let storage_changeset_worker = thread::spawn(move || {
            write_storage_changesets(storage_changeset_factory, storage_changeset_collectors)
        });

        let trie_provider_factory = provider_factory.clone();
        let storage_history_factory = provider_factory;
        let storage_history_worker = thread::spawn(move || {
            write_storage_history(storage_history_factory, storage_history_collectors, workers)
        });

        // Load sorted entries from each ETL collector into its database table.
        // Strategy: iterate the sorted collector, deduplicate consecutive entries with
        // the same composite key, and bulk-insert via append_dup.
        // The table is cleared first so append_dup ordering is guaranteed.
        let total_hashes = hashed_collectors
            .iter()
            .map(|collector| collector.len())
            .sum::<usize>();
        provider_rw.tx_ref().clear::<tables::HashedStorages>()?;
        let mut hashed_cursor = provider_rw
            .tx_ref()
            .cursor_dup_write::<tables::HashedStorages>()?;
        load_etl_collectors_to_cursor(
            &mut hashed_collectors,
            total_hashes,
            "hashed storage",
            |k, v| {
                hashed_cursor.append_dup(
                    B256::from_slice(&k[..32]),
                    StorageEntry {
                        key: B256::from_slice(&k[32..]),
                        value: v,
                    },
                )
            },
        )?;
        drop(hashed_cursor);

        info!(
            target: "tempo::cli",
            total_hashes,
            "Storage written, writing hashed accounts..."
        );

        // Write hashed account entries using the real account metadata from plain state.
        // This preserves bytecode_hash for genesis accounts (e.g. TIP20 tokens with 0xEF code).
        provider_rw.insert_account_for_hashing(
            accounts_seen
                .iter()
                .map(|(addr, account)| (*addr, Some(*account))),
        )?;

        storage_changeset_worker
            .join()
            .map_err(|_| eyre::eyre!("storage changeset worker panicked"))??;
        storage_history_worker
            .join()
            .map_err(|_| eyre::eyre!("storage history worker panicked"))??;

        info!(
            target: "tempo::cli",
            addresses = accounts_seen.len(),
            workers,
            "Hashed accounts written, computing state root and trie nodes..."
        );

        // Rebuild the merkle trie from scratch so the sparse trie cache on
        // block 1 doesn't hit stale genesis nodes and stall on a full rebuild.
        provider_rw.tx_ref().clear::<tables::AccountsTrie>()?;
        provider_rw.tx_ref().clear::<tables::StoragesTrie>()?;
        let state_root = if use_parallel_trie(workers) {
            info!(
                target: "tempo::cli",
                "Committing loaded state before parallel trie root"
            );
            provider_rw.commit()?;
            let provider_rw = trie_provider_factory.database_provider_rw()?;
            let state_root =
                compute_and_write_trie_root(trie_provider_factory, &provider_rw, workers)?;
            provider_rw.commit()?;
            state_root
        } else {
            let state_root =
                compute_and_write_trie_root(trie_provider_factory, &provider_rw, workers)?;
            provider_rw.commit()?;
            state_root
        };

        info!(
            target: "tempo::cli",
            total_blocks,
            total_entries,
            %state_root,
            "Binary state dump loaded successfully"
        );

        Ok(())
    }
}

fn compute_and_write_trie_root<P, RW>(
    provider_factory: P,
    provider_rw: &RW,
    trie_workers: usize,
) -> eyre::Result<B256>
where
    P: DatabaseProviderFactory + ChainSpecProvider + Clone + Send + 'static,
    P::Provider: StageCheckpointReader
        + PruneCheckpointReader
        + ChangeSetReader
        + StorageChangeSetReader
        + DBProvider
        + BlockNumReader
        + StorageSettingsCache,
    RW: DBProvider + TrieWriter + ChainSpecProvider,
    RW::ChainSpec: EthChainSpec,
    RW::Tx: DbTxMut,
{
    if use_parallel_trie(trie_workers) {
        compute_and_write_parallel_trie_root(provider_factory, provider_rw, trie_workers)
    } else {
        compute_and_write_serial_trie_root(provider_rw)
    }
    .or_else(|err| {
        if use_parallel_trie(trie_workers) {
            debug!(
                target: "tempo::cli",
                ?err,
                "Parallel trie root failed; retrying with serial trie root"
            );
            compute_and_write_serial_trie_root(provider_rw)
        } else {
            Err(err)
        }
    })
    .map(|(state_root, trie_writes, elapsed, mode)| {
        info!(
            target: "tempo::cli",
            %state_root,
            trie_writes,
            elapsed = ?elapsed,
            %mode,
            "State root computed"
        );
        state_root
    })
}

fn use_parallel_trie(trie_workers: usize) -> bool {
    trie_workers > 1
}

fn compute_and_write_serial_trie_root<RW>(
    provider_rw: &RW,
) -> eyre::Result<(B256, usize, Duration, &'static str)>
where
    RW: DBProvider + TrieWriter,
    RW::Tx: DbTxMut,
{
    let trie_start = Instant::now();
    let mut resume: Option<IntermediateStateRootState> = None;
    let mut trie_writes = 0usize;

    use reth_trie_db::{DatabaseHashedCursorFactory, DatabaseTrieCursorFactory, PackedKeyAdapter};
    type DbStateRoot<'a, TX> = reth_trie::StateRoot<
        DatabaseTrieCursorFactory<&'a TX, PackedKeyAdapter>,
        DatabaseHashedCursorFactory<&'a TX>,
    >;

    let state_root = loop {
        match DbStateRoot::<_>::from_tx(provider_rw.tx_ref())
            .with_intermediate_state(resume)
            .root_with_progress()?
        {
            StateRootProgress::Progress(state, _, updates) => {
                trie_writes += provider_rw.write_trie_updates(updates)?;
                info!(
                    target: "tempo::cli",
                    last_key = %state.account_root_state.last_hashed_key,
                    trie_writes,
                    elapsed = ?trie_start.elapsed(),
                    "Flushing trie updates"
                );
                resume = Some(*state);
            }
            StateRootProgress::Complete(root, _, updates) => {
                trie_writes += provider_rw.write_trie_updates(updates)?;
                break root;
            }
        }
    };

    Ok((state_root, trie_writes, trie_start.elapsed(), "serial"))
}

fn compute_and_write_parallel_trie_root<P, RW>(
    provider_factory: P,
    provider_rw: &RW,
    trie_workers: usize,
) -> eyre::Result<(B256, usize, Duration, &'static str)>
where
    P: DatabaseProviderFactory + ChainSpecProvider + Clone + Send + 'static,
    P::Provider: StageCheckpointReader
        + PruneCheckpointReader
        + ChangeSetReader
        + StorageChangeSetReader
        + DBProvider
        + BlockNumReader
        + StorageSettingsCache,
    RW: DBProvider + TrieWriter + ChainSpecProvider,
    RW::ChainSpec: EthChainSpec,
    RW::Tx: DbTxMut,
{
    let trie_start = Instant::now();
    let prefix_sets = collect_full_trie_prefix_sets(provider_rw)?;
    let genesis_hash = provider_rw.chain_spec().genesis_hash();
    let (state_root, updates) = thread::Builder::new()
        .name("tempo-parallel-trie-root".to_string())
        .spawn(move || -> eyre::Result<_> {
            let runtime = trie_runtime(trie_workers)?;
            let overlay_builder = OverlayBuilder::<TempoPrimitives>::new(
                genesis_hash,
                reth_trie_db::ChangesetCache::new(),
            );
            let overlay_factory =
                OverlayStateProviderFactory::new(provider_factory, overlay_builder);
            parallel_state_root_with_updates(overlay_factory, prefix_sets, runtime)
        })
        .wrap_err("failed to spawn parallel trie root worker")?
        .join()
        .map_err(|_| eyre::eyre!("parallel trie root worker panicked"))??;
    let trie_writes = provider_rw.write_trie_updates(updates)?;

    Ok((state_root, trie_writes, trie_start.elapsed(), "parallel"))
}

fn parallel_state_root_with_updates<Factory>(
    factory: Factory,
    prefix_sets: TriePrefixSets,
    runtime: Runtime,
) -> eyre::Result<(B256, TrieUpdates)>
where
    Factory: DatabaseProviderROFactory<Provider: TrieCursorFactory + HashedCursorFactory>
        + Clone
        + Send
        + 'static,
{
    let TriePrefixSets {
        account_prefix_set,
        storage_prefix_sets,
        destroyed_accounts,
    } = prefix_sets;
    let mut storage_root_targets = StorageRootTargets::new(
        account_prefix_set
            .iter()
            .map(|nibbles| B256::from_slice(&nibbles.pack())),
        storage_prefix_sets,
    )
    .into_iter()
    .collect::<Vec<_>>();
    storage_root_targets.sort_unstable_by_key(|(address, _)| *address);

    let handle = runtime.handle().clone();
    let mut storage_roots =
        B256Map::with_capacity_and_hasher(storage_root_targets.len(), Default::default());

    for (hashed_address, prefix_set) in storage_root_targets {
        let factory = factory.clone();
        let metrics = TrieRootMetrics::new(TrieType::Storage);
        let (tx, rx) = mpsc::sync_channel(1);
        drop(handle.spawn_blocking(move || {
            let result = (|| -> eyre::Result<_> {
                let provider = factory.database_provider_ro()?;
                let (storage_root, _, updates) = StorageRoot::new_hashed(
                    &provider,
                    &provider,
                    hashed_address,
                    prefix_set,
                    metrics,
                )
                .root_with_updates()?;
                Ok((storage_root, updates))
            })();
            let _ = tx.send(result);
        }));
        storage_roots.insert(hashed_address, rx);
    }

    let provider = factory.database_provider_ro()?;
    let walker = TrieWalker::<_>::state_trie(provider.account_trie_cursor()?, account_prefix_set)
        .with_deletions_retained(true);
    let mut account_node_iter = TrieNodeIter::state_trie(walker, provider.hashed_account_cursor()?);

    let mut trie_updates = TrieUpdates::default();
    let mut hash_builder = HashBuilder::default().with_updates(true);
    let mut account_rlp = Vec::with_capacity(TRIE_ACCOUNT_RLP_MAX_SIZE);

    while let Some(node) = account_node_iter.try_next()? {
        match node {
            TrieElement::Branch(node) => {
                hash_builder.add_branch(node.key, node.value, node.children_are_in_trie);
            }
            TrieElement::Leaf(hashed_address, account) => {
                let (storage_root, updates) = match storage_roots.remove(&hashed_address) {
                    Some(rx) => rx.recv().map_err(|_| {
                        eyre::eyre!("storage root worker channel closed for {hashed_address}")
                    })??,
                    None => {
                        let (storage_root, _, updates) = StorageRoot::new_hashed(
                            &provider,
                            &provider,
                            hashed_address,
                            Default::default(),
                            TrieRootMetrics::new(TrieType::Storage),
                        )
                        .root_with_updates()?;
                        (storage_root, updates)
                    }
                };

                trie_updates.insert_storage_updates(hashed_address, updates);
                account_rlp.clear();
                let account = account.into_trie_account(storage_root);
                account.encode(&mut account_rlp as &mut dyn BufMut);
                hash_builder.add_leaf(Nibbles::unpack(hashed_address), &account_rlp);
            }
        }
    }

    let root = hash_builder.root();
    let removed_keys = account_node_iter.walker.take_removed_keys();
    trie_updates.finalize(hash_builder, removed_keys, destroyed_accounts);

    Ok((root, trie_updates))
}

fn trie_runtime(trie_workers: usize) -> eyre::Result<Runtime> {
    RuntimeBuilder::new(
        RuntimeConfig::default().with_tokio(TokioConfig::with_worker_threads(trie_workers)),
    )
    .build()
    .wrap_err_with(|| format!("failed to build trie runtime with {trie_workers} workers"))
}

fn collect_full_trie_prefix_sets<P>(provider: &P) -> eyre::Result<TriePrefixSets>
where
    P: DBProvider,
    P::Tx: DbTx,
{
    let mut cursor = provider.tx_ref().cursor_read::<tables::HashedAccounts>()?;
    let mut hashed_addresses = Vec::new();
    let mut hashed_accounts = 0usize;
    for row in cursor.walk(None)? {
        let (hashed_address, _) = row?;
        hashed_addresses.push(hashed_address);
        hashed_accounts += 1;
    }
    info!(
        target: "tempo::cli",
        hashed_accounts,
        "Collected trie prefixes for parallel state root"
    );
    Ok(full_trie_prefix_sets_from_hashed_accounts(hashed_addresses))
}

fn full_trie_prefix_sets_from_hashed_accounts(
    hashed_addresses: impl IntoIterator<Item = B256>,
) -> TriePrefixSets {
    let mut prefix_sets = TriePrefixSetsMut::default();
    for hashed_address in hashed_addresses {
        prefix_sets
            .account_prefix_set
            .insert(Nibbles::unpack(hashed_address));
    }
    prefix_sets.freeze()
}

struct CollectionChunk {
    address: alloy_primitives::Address,
    entries: Vec<DumpEntry>,
}

impl CollectionChunk {
    fn new(address: alloy_primitives::Address) -> Self {
        Self {
            address,
            entries: Vec::with_capacity(COLLECTION_CHUNK_SIZE),
        }
    }
}

struct CollectionWorkerResult {
    hashed_collector: StorageCollector,
    storage_changeset_collector: StorageCollector,
    storage_history_collector: StorageCollector,
    chunks: usize,
    pairs: usize,
    entries: usize,
}

struct CollectionWorkerPool {
    senders: Vec<mpsc::SyncSender<CollectionChunk>>,
    handles: Vec<thread::JoinHandle<eyre::Result<CollectionWorkerResult>>>,
    next_worker: usize,
}

impl CollectionWorkerPool {
    fn new(
        worker_count: usize,
        etl_dir: PathBuf,
        genesis_storage_keys: Arc<HashSet<Vec<u8>>>,
    ) -> eyre::Result<Self> {
        let worker_count = worker_count.max(1);
        let mut senders = Vec::with_capacity(worker_count);
        let mut handles = Vec::with_capacity(worker_count);

        for worker_id in 0..worker_count {
            let (tx, rx) = mpsc::sync_channel::<CollectionChunk>(COLLECTION_WORKER_QUEUE_DEPTH);
            let worker_etl_dir = etl_dir.clone();
            let worker_genesis_storage_keys = Arc::clone(&genesis_storage_keys);
            let handle = thread::Builder::new()
                .name(format!("tempo-state-collect-{worker_id}"))
                .spawn(move || {
                    collection_worker_loop(
                        rx,
                        worker_etl_dir,
                        ETL_FILE_SIZE,
                        worker_genesis_storage_keys,
                    )
                })
                .wrap_err("failed to spawn collection worker")?;
            senders.push(tx);
            handles.push(handle);
        }

        Ok(Self {
            senders,
            handles,
            next_worker: 0,
        })
    }

    fn send(&mut self, chunk: CollectionChunk) -> eyre::Result<()> {
        if chunk.entries.is_empty() {
            return Ok(());
        }

        let worker = self.next_worker;
        self.next_worker = (self.next_worker + 1) % self.senders.len();
        self.senders[worker]
            .send(chunk)
            .map_err(|_| eyre::eyre!("collection worker disconnected"))?;
        Ok(())
    }

    fn finish(self) -> eyre::Result<Vec<CollectionWorkerResult>> {
        let Self {
            senders,
            handles,
            next_worker: _,
        } = self;
        drop(senders);

        let mut results = Vec::with_capacity(handles.len());
        for handle in handles {
            results.push(
                handle
                    .join()
                    .map_err(|_| eyre::eyre!("collection worker panicked"))??,
            );
        }
        Ok(results)
    }
}

fn collection_worker_loop(
    rx: mpsc::Receiver<CollectionChunk>,
    etl_dir: PathBuf,
    etl_file_size: usize,
    genesis_storage_keys: Arc<HashSet<Vec<u8>>>,
) -> eyre::Result<CollectionWorkerResult> {
    let mut hashed_collector = Collector::new(etl_file_size, Some(etl_dir.clone()));
    let mut storage_changeset_collector = Collector::new(etl_file_size, Some(etl_dir.clone()));
    let mut storage_history_collector = Collector::new(etl_file_size, Some(etl_dir));
    let mut chunks = 0usize;
    let mut pairs = 0usize;
    let mut entries = 0usize;

    while let Ok(chunk) = rx.recv() {
        chunks += 1;
        pairs += chunk.entries.len();
        entries += collect_dump_chunk(
            chunk,
            &genesis_storage_keys,
            &mut hashed_collector,
            &mut storage_changeset_collector,
            &mut storage_history_collector,
        )?;
    }

    Ok(CollectionWorkerResult {
        hashed_collector,
        storage_changeset_collector,
        storage_history_collector,
        chunks,
        pairs,
        entries,
    })
}

fn collect_dump_chunk(
    chunk: CollectionChunk,
    genesis_storage_keys: &HashSet<Vec<u8>>,
    hashed_collector: &mut StorageCollector,
    storage_changeset_collector: &mut StorageCollector,
    storage_history_collector: &mut StorageCollector,
) -> eyre::Result<usize> {
    let hashed_addr = keccak256(chunk.address);
    let zero = CompactU256::from(U256::ZERO);
    let mut entries = 0usize;

    for entry in chunk.entries {
        if entry[32..].iter().all(|byte| *byte == 0) {
            continue;
        }

        let slot = B256::from_slice(&entry[..32]);
        let value = U256::from_be_bytes::<32>(entry[32..64].try_into().unwrap());
        let compact_value = CompactU256::from(value);

        let raw_key = raw_storage_key(chunk.address, slot);
        if !genesis_storage_keys.contains(raw_key.as_slice()) {
            storage_changeset_collector
                .insert(raw_key.clone(), zero.clone())
                .wrap_err("storage changeset ETL insert failed")?;
            storage_history_collector
                .insert(raw_key, zero.clone())
                .wrap_err("storage history ETL insert failed")?;
        }

        let mut hashed_key = Vec::with_capacity(64);
        hashed_key.extend_from_slice(hashed_addr.as_slice());
        hashed_key.extend_from_slice(keccak256(slot).as_slice());
        hashed_collector
            .insert(hashed_key, compact_value)
            .wrap_err("hashed ETL insert failed")?;

        entries += 1;
    }

    Ok(entries)
}

fn write_storage_changesets<P>(
    provider: P,
    mut collectors: Vec<StorageCollector>,
) -> eyre::Result<()>
where
    P: StaticFileProviderFactory + Send + 'static,
{
    info!(target: "tempo::cli", "Writing storage changesets...");

    provider
        .static_file_provider()
        .delete_segment(StaticFileSegment::StorageChangeSets)?;

    let mut writer = provider.get_static_file_writer(0, StaticFileSegment::StorageChangeSets)?;
    writer.begin_storage_changeset(0)?;
    let total = collectors.iter().map(|collector| collector.len()).sum();
    load_storage_etl_collectors(
        &mut collectors,
        total,
        "storage changeset",
        |address, key, value| {
            writer.append_storage_changeset_entry(StorageBeforeTx {
                address,
                key,
                value,
            })?;
            Ok(())
        },
    )?;
    drop(writer);

    Ok(())
}

fn write_storage_history<P>(
    provider: P,
    mut collectors: Vec<StorageCollector>,
    worker_count: usize,
) -> eyre::Result<()>
where
    P: RocksDBProviderFactory + Send + 'static,
{
    info!(target: "tempo::cli", worker_count, "Writing storage history...");

    let rocksdb = provider.rocksdb_provider();
    let total = collectors.iter().map(|collector| collector.len()).sum();
    if worker_count <= 1 {
        let mut batch = rocksdb.batch_with_auto_commit();
        let block_zero_history =
            tables::BlockNumberList::new([0]).expect("single block is always sorted");
        let entries = load_storage_history_etl_collectors(
            &mut collectors,
            total,
            "storage history",
            |entry| write_storage_history_entry(&mut batch, &block_zero_history, entry),
        )?;
        batch.commit()?;
        info!(target: "tempo::cli", entries, "Storage history written");
    } else {
        let mut pool = StorageHistoryWorkerPool::new(worker_count, rocksdb)?;
        let entries = load_storage_history_etl_collectors(
            &mut collectors,
            total,
            "storage history",
            |entry| pool.send(entry),
        )?;
        let results = pool.finish()?;
        let active_workers = results.iter().filter(|result| result.entries > 0).count();
        let worker_entries: usize = results.iter().map(|result| result.entries).sum();
        let worker_chunks: usize = results.iter().map(|result| result.chunks).sum();
        info!(
            target: "tempo::cli",
            worker_count = results.len(),
            active_workers,
            worker_chunks,
            worker_entries,
            entries,
            "Storage history workers finished"
        );
    }

    Ok(())
}

struct StorageHistoryWorkerResult {
    chunks: usize,
    entries: usize,
}

struct StorageHistoryWorkerPool {
    senders: Vec<mpsc::SyncSender<StorageHistoryChunk>>,
    handles: Vec<thread::JoinHandle<eyre::Result<StorageHistoryWorkerResult>>>,
    buffers: Vec<StorageHistoryChunk>,
}

impl StorageHistoryWorkerPool {
    fn new(
        worker_count: usize,
        rocksdb: reth_provider::providers::RocksDBProvider,
    ) -> eyre::Result<Self> {
        let worker_count = worker_count.max(1);
        let mut senders = Vec::with_capacity(worker_count);
        let mut handles = Vec::with_capacity(worker_count);
        let mut buffers = Vec::with_capacity(worker_count);

        for worker_id in 0..worker_count {
            let (tx, rx) =
                mpsc::sync_channel::<StorageHistoryChunk>(STORAGE_HISTORY_WORKER_QUEUE_DEPTH);
            let worker_rocksdb = rocksdb.clone();
            let handle = thread::Builder::new()
                .name(format!("tempo-storage-history-{worker_id}"))
                .spawn(move || storage_history_worker_loop(rx, worker_rocksdb))
                .wrap_err("failed to spawn storage history worker")?;
            senders.push(tx);
            handles.push(handle);
            buffers.push(Vec::with_capacity(STORAGE_HISTORY_CHUNK_SIZE));
        }

        Ok(Self {
            senders,
            handles,
            buffers,
        })
    }

    fn send(&mut self, entry: StorageHistoryEntry) -> eyre::Result<()> {
        let worker = storage_history_shard(entry.0, entry.1, self.senders.len());
        self.buffers[worker].push(entry);
        if self.buffers[worker].len() >= STORAGE_HISTORY_CHUNK_SIZE {
            self.flush_worker(worker)?;
        }
        Ok(())
    }

    fn flush_worker(&mut self, worker: usize) -> eyre::Result<()> {
        if self.buffers[worker].is_empty() {
            return Ok(());
        }
        let chunk = std::mem::replace(
            &mut self.buffers[worker],
            Vec::with_capacity(STORAGE_HISTORY_CHUNK_SIZE),
        );
        self.senders[worker]
            .send(chunk)
            .map_err(|_| eyre::eyre!("storage history worker disconnected"))?;
        Ok(())
    }

    fn finish(mut self) -> eyre::Result<Vec<StorageHistoryWorkerResult>> {
        for worker in 0..self.buffers.len() {
            self.flush_worker(worker)?;
        }

        let Self {
            senders,
            handles,
            buffers: _,
        } = self;
        drop(senders);

        let mut results = Vec::with_capacity(handles.len());
        for handle in handles {
            results.push(
                handle
                    .join()
                    .map_err(|_| eyre::eyre!("storage history worker panicked"))??,
            );
        }
        Ok(results)
    }
}

fn storage_history_worker_loop(
    rx: mpsc::Receiver<StorageHistoryChunk>,
    rocksdb: reth_provider::providers::RocksDBProvider,
) -> eyre::Result<StorageHistoryWorkerResult> {
    let mut batch = rocksdb.batch_with_auto_commit();
    let block_zero_history =
        tables::BlockNumberList::new([0]).expect("single block is always sorted");
    let mut chunks = 0usize;
    let mut entries = 0usize;

    while let Ok(chunk) = rx.recv() {
        chunks += 1;
        entries += chunk.len();
        for entry in chunk {
            write_storage_history_entry(&mut batch, &block_zero_history, entry)?;
        }
    }
    batch.commit()?;

    Ok(StorageHistoryWorkerResult { chunks, entries })
}

fn write_storage_history_entry(
    batch: &mut reth_provider::providers::RocksDBBatch<'_>,
    block_zero_history: &tables::BlockNumberList,
    (address, key): StorageHistoryEntry,
) -> eyre::Result<()> {
    let history_key = StorageShardedKey::last(address, key);
    if batch
        .get::<tables::StoragesHistory>(history_key.clone())?
        .is_none()
    {
        batch.put::<tables::StoragesHistory>(history_key, block_zero_history)?;
    }
    Ok(())
}

fn storage_history_shard(
    _address: alloy_primitives::Address,
    key: B256,
    worker_count: usize,
) -> usize {
    if worker_count <= 1 {
        return 0;
    }
    let mut shard_bytes = [0u8; 8];
    shard_bytes.copy_from_slice(&key.as_slice()[24..32]);
    (u64::from_be_bytes(shard_bytes) as usize) % worker_count
}

/// Composite ETL key for unhashed storage, sorted by address then slot.
fn raw_storage_key(address: alloy_primitives::Address, slot: B256) -> Vec<u8> {
    let mut key = Vec::with_capacity(52);
    key.extend_from_slice(address.as_slice());
    key.extend_from_slice(slot.as_slice());
    key
}

fn decode_raw_storage_key(key: &[u8]) -> (alloy_primitives::Address, B256) {
    (
        alloy_primitives::Address::from_slice(&key[..20]),
        B256::from_slice(&key[20..]),
    )
}

/// Iterate raw storage ETL collectors, deduplicate consecutive entries with the
/// same `(address, slot)` key, and call `write` for each unique entry.
fn load_storage_etl_collectors(
    collectors: &mut [StorageCollector],
    total: usize,
    label: &str,
    mut write: impl FnMut(alloy_primitives::Address, B256, U256) -> eyre::Result<()>,
) -> eyre::Result<()> {
    load_etl_collectors(collectors, total, label, |key, value| {
        let (address, storage_key) = decode_raw_storage_key(&key);
        write(
            address,
            storage_key,
            CompactU256::decompress_owned(value)?.into(),
        )
    })
}

fn load_storage_history_etl_collectors(
    collectors: &mut [StorageCollector],
    total: usize,
    label: &str,
    mut write: impl FnMut(StorageHistoryEntry) -> eyre::Result<()>,
) -> eyre::Result<usize> {
    let mut entries = 0usize;
    load_etl_collectors(collectors, total, label, |key, _| {
        write(decode_raw_storage_key(&key))?;
        entries += 1;
        Ok(())
    })?;
    Ok(entries)
}

/// Iterate sorted ETL collectors, deduplicate consecutive entries with the same key
/// (keeping the last value), and call `append` for each unique entry.
fn load_etl_collectors_to_cursor(
    collectors: &mut [StorageCollector],
    total: usize,
    label: &str,
    mut append: impl FnMut(&[u8], U256) -> Result<(), reth_db_api::DatabaseError>,
) -> eyre::Result<()> {
    load_etl_collectors(collectors, total, label, |key, value| {
        append(&key, CompactU256::decompress_owned(value)?.into()).wrap_err("cursor append failed")
    })
}

fn load_etl_collectors(
    collectors: &mut [StorageCollector],
    total: usize,
    label: &str,
    mut write: impl FnMut(Vec<u8>, Vec<u8>) -> eyre::Result<()>,
) -> eyre::Result<()> {
    let total = total.max(1);
    let interval = (total / 10).max(1);
    let mut iterators = Vec::with_capacity(collectors.len());
    for collector in collectors.iter_mut() {
        iterators.push(collector.iter()?);
    }

    let mut heap = BinaryHeap::<(Reverse<(Vec<u8>, Vec<u8>)>, usize)>::new();
    for (collector_id, iterator) in iterators.iter_mut().enumerate() {
        if let Some(item) = iterator.next() {
            let (key, value) = item.wrap_err("ETL iteration failed")?;
            heap.push((Reverse((key, value)), collector_id));
        }
    }

    let mut pending: Option<(Vec<u8>, Vec<u8>)> = None;
    let mut index = 0usize;
    while let Some((Reverse((key, value)), collector_id)) = heap.pop() {
        if index > 0 && index % interval == 0 {
            info!(
                target: "tempo::cli",
                progress = format_args!("{:.2}%", (index as f64 / total as f64) * 100.0),
                "Inserting {label}"
            );
        }

        if let Some((prev_key, prev_val)) = pending.take()
            && prev_key != key
        {
            write(prev_key, prev_val)?;
        }
        pending = Some((key, value));

        if let Some(item) = iterators[collector_id].next() {
            let (key, value) = item.wrap_err("ETL iteration failed")?;
            heap.push((Reverse((key, value)), collector_id));
        }

        index += 1;
    }
    if let Some((key, val)) = pending {
        write(key, val)?;
    }
    Ok(())
}

/// Log collection progress every 5 seconds and on the final entry.
fn log_collection_progress(
    address: &alloy_primitives::Address,
    index: u64,
    total: u64,
    start: Instant,
    last_log: &mut Instant,
) {
    if last_log.elapsed() >= Duration::from_secs(5) || index + 1 == total {
        let pct = ((index + 1) as f64 / total as f64) * 100.0;
        let elapsed = start.elapsed();
        let pairs_per_sec = (index + 1) as f64 / elapsed.as_secs_f64();
        info!(
            target: "tempo::cli",
            %address,
            progress = format_args!("{}/{} ({pct:.0}%)", index + 1, total),
            elapsed = ?elapsed,
            pairs_per_sec = pairs_per_sec as u64,
            "Collecting storage"
        );
        *last_log = Instant::now();
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use alloy_primitives::Address;
    use reth_db_api::table::{Compress, Encode};
    use reth_trie_parallel::StorageRootTargets;

    fn address(value: u64) -> Address {
        let mut bytes = [0u8; 20];
        bytes[12..].copy_from_slice(&value.to_be_bytes());
        Address::from(bytes)
    }

    fn slot(value: u64) -> B256 {
        let mut bytes = [0u8; 32];
        bytes[24..].copy_from_slice(&value.to_be_bytes());
        B256::from(bytes)
    }

    fn dump_entry(slot: B256, value: U256) -> DumpEntry {
        let mut entry = [0u8; 64];
        entry[..32].copy_from_slice(slot.as_slice());
        entry[32..].copy_from_slice(&value.to_be_bytes::<32>());
        entry
    }

    #[test]
    fn collect_dump_chunk_filters_zeroes_and_skips_genesis_raw_keys() -> eyre::Result<()> {
        let tempdir = tempfile::tempdir()?;
        let account = address(7);
        let new_slot = slot(1);
        let zero_slot = slot(2);
        let genesis_slot = slot(3);

        let mut genesis_storage_keys = HashSet::new();
        genesis_storage_keys.insert(raw_storage_key(account, genesis_slot));

        let chunk = CollectionChunk {
            address: account,
            entries: vec![
                dump_entry(new_slot, U256::from(11)),
                dump_entry(zero_slot, U256::ZERO),
                dump_entry(genesis_slot, U256::from(33)),
            ],
        };

        let mut hashed_collector = Collector::new(128, Some(tempdir.path().to_path_buf()));
        let mut changeset_collector = Collector::new(128, Some(tempdir.path().to_path_buf()));
        let mut history_collector = Collector::new(128, Some(tempdir.path().to_path_buf()));

        let entries = collect_dump_chunk(
            chunk,
            &genesis_storage_keys,
            &mut hashed_collector,
            &mut changeset_collector,
            &mut history_collector,
        )?;

        assert_eq!(entries, 2);

        let changeset_total = changeset_collector.len();
        let mut changeset_output = Vec::new();
        load_storage_etl_collectors(
            std::slice::from_mut(&mut changeset_collector),
            changeset_total,
            "test changeset",
            |address, key, value| {
                changeset_output.push((address, key, value));
                Ok(())
            },
        )?;

        assert_eq!(changeset_output, vec![(account, new_slot, U256::ZERO)]);

        let history_total = history_collector.len();
        let mut history_output = Vec::new();
        load_storage_etl_collectors(
            std::slice::from_mut(&mut history_collector),
            history_total,
            "test history",
            |address, key, value| {
                history_output.push((address, key, value));
                Ok(())
            },
        )?;

        assert_eq!(history_output, vec![(account, new_slot, U256::ZERO)]);

        let hashed_total = hashed_collector.len();
        let mut hashed_output = Vec::new();
        load_etl_collectors_to_cursor(
            std::slice::from_mut(&mut hashed_collector),
            hashed_total,
            "test hashed",
            |key, value| {
                hashed_output.push((key.to_vec(), value));
                Ok::<_, reth_db_api::DatabaseError>(())
            },
        )?;

        assert_eq!(hashed_output.len(), 2);
        assert!(
            hashed_output
                .iter()
                .any(|(_, value)| *value == U256::from(11))
        );
        assert!(
            hashed_output
                .iter()
                .any(|(_, value)| *value == U256::from(33))
        );

        Ok(())
    }

    fn sample_storage_history_collector() -> eyre::Result<Collector<Vec<u8>, CompactU256>> {
        let tempdir = tempfile::tempdir()?;
        let mut collector = Collector::new(128, Some(tempdir.keep()));
        let entries = [
            (address(1), slot(9), U256::from(1)),
            (address(1), slot(7), U256::from(2)),
            (address(1), slot(7), U256::from(3)),
            (address(2), slot(8), U256::from(4)),
            (address(2), slot(8), U256::from(5)),
            (address(2), slot(1), U256::from(6)),
            (address(3), slot(5), U256::from(7)),
        ];
        for (address, slot, value) in entries {
            collector.insert(raw_storage_key(address, slot), CompactU256::from(value))?;
        }
        Ok(collector)
    }

    fn encoded_storage_history_entry(entry: StorageHistoryEntry) -> (Vec<u8>, Vec<u8>) {
        let block_zero_history =
            tables::BlockNumberList::new([0]).expect("single block is always sorted");
        let value: Vec<u8> = block_zero_history.compress();
        (
            StorageShardedKey::last(entry.0, entry.1)
                .encode()
                .as_ref()
                .to_vec(),
            value,
        )
    }

    fn collect_storage_history_format(
        worker_count: usize,
    ) -> eyre::Result<Vec<(Vec<u8>, Vec<u8>)>> {
        let mut collector = sample_storage_history_collector()?;
        let total = collector.len();
        let mut output = Vec::new();

        if worker_count <= 1 {
            load_storage_history_etl_collectors(
                std::slice::from_mut(&mut collector),
                total,
                "test storage history",
                |entry| {
                    output.push(encoded_storage_history_entry(entry));
                    Ok(())
                },
            )?;
        } else {
            let mut shards = vec![Vec::new(); worker_count];
            load_storage_history_etl_collectors(
                std::slice::from_mut(&mut collector),
                total,
                "test storage history",
                |entry| {
                    let shard = storage_history_shard(entry.0, entry.1, worker_count);
                    shards[shard].push(encoded_storage_history_entry(entry));
                    Ok(())
                },
            )?;
            output = shards.into_iter().flatten().collect();
            output.sort();
        }

        Ok(output)
    }

    #[test]
    fn parallel_storage_history_preserves_single_worker_format() -> eyre::Result<()> {
        let single_worker = collect_storage_history_format(1)?;
        let parallel_workers = collect_storage_history_format(4)?;

        assert_eq!(single_worker, parallel_workers);
        assert_eq!(single_worker.len(), 5);

        Ok(())
    }

    #[test]
    fn storage_history_sharding_distributes_by_storage_key() {
        let worker_count = 4;
        let shards: Vec<_> = (0..64)
            .map(|index| storage_history_shard(address(1), slot(index), worker_count))
            .collect();
        let active_workers = (0..worker_count)
            .filter(|worker| shards.iter().any(|shard| shard == worker))
            .count();

        assert!(active_workers > 1);
        assert_eq!(
            storage_history_shard(address(1), slot(7), worker_count),
            storage_history_shard(address(99), slot(7), worker_count)
        );
    }

    #[test]
    fn workers_parser_accepts_auto_and_positive_counts() {
        assert_eq!(parse_workers("auto").unwrap(), WorkerSetting::Auto);
        assert_eq!(parse_workers("AUTO").unwrap(), WorkerSetting::Auto);
        assert_eq!(parse_workers("1").unwrap(), WorkerSetting::Count(1));
        assert_eq!(parse_workers("8").unwrap(), WorkerSetting::Count(8));
        assert!(parse_workers("0").is_err());
        assert!(parse_workers("many").is_err());
    }

    #[test]
    fn workers_auto_resolves_to_at_least_one_worker() {
        assert!(WorkerSetting::Auto.resolve() >= 1);
    }

    #[test]
    fn worker_parallelism_is_opt_in() {
        assert!(!use_parallel_trie(0));
        assert!(!use_parallel_trie(1));
        assert!(use_parallel_trie(2));
        assert!(use_parallel_trie(8));
    }

    #[test]
    fn workers_cli_defaults_to_auto() {
        let cmd = InitFromBinaryDump::<TempoChainSpecParser>::try_parse_from([
            "tempo",
            "state_bloat.bin",
        ])
        .unwrap();

        assert_eq!(cmd.workers, WorkerSetting::Auto);
        assert_eq!(cmd.state, PathBuf::from("state_bloat.bin"));
    }

    #[test]
    fn workers_cli_accepts_parallelism() {
        let cmd = InitFromBinaryDump::<TempoChainSpecParser>::try_parse_from([
            "tempo",
            "--workers",
            "8",
            "state_bloat.bin",
        ])
        .unwrap();

        assert_eq!(cmd.workers, WorkerSetting::Count(8));
        assert_eq!(cmd.state, PathBuf::from("state_bloat.bin"));
    }

    #[test]
    fn full_trie_prefix_sets_select_every_hashed_account_for_storage_precompute() {
        let account_a = B256::with_last_byte(0x11);
        let account_b = B256::with_last_byte(0x22);
        let account_c = B256::with_last_byte(0x33);

        let prefix_sets = full_trie_prefix_sets_from_hashed_accounts([
            account_b, account_a, account_b, account_c,
        ]);

        let collected = prefix_sets
            .account_prefix_set
            .iter()
            .map(|nibbles| B256::from_slice(&nibbles.pack()))
            .collect::<Vec<_>>();

        assert_eq!(collected, vec![account_a, account_b, account_c]);
        assert!(prefix_sets.storage_prefix_sets.is_empty());
        assert_eq!(
            StorageRootTargets::count(
                &prefix_sets.account_prefix_set,
                &prefix_sets.storage_prefix_sets,
            ),
            3
        );
    }
}
