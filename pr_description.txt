The T3 gas parameter splits used a flat `T3_EXEC_GAS = 5,000` for all
operations instead of the per-operation values from the TIP-1016 spec table.
This made storage operations appear cheaper against protocol limits than
standard EVM, violating invariant 15.

Corrected regular/state splits to match the spec:
- SSTORE: 20k/230k (was 5k/245k)
- Account creation: 25k/225k (was 5k/245k)
- CREATE base: 32k/468k (was 5k/495k)
- EIP-7702 per auth: 250k total (was 12.5k, TIP-1000 value)
- SSTORE set refund: 247,800 (was Berlin default 19,900)

Code deposit (200/2,300) was already correct.
