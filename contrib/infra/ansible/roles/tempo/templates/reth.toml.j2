# Minimal Reth configuration
[stages]
[prune]

[peers]
trusted_nodes = [
{% for host in play_hosts %}
  "enode://{{ hostvars[host]['disc_pub_key_hex'] }}@{{ hostvars[host]['tempo_ovh_private_address'] | default(host) }}:30303"{% if not loop.last %},{% endif %}

{% endfor %}
]