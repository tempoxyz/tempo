- name: Bootstrap system
  hosts: all
  tags: bootstrap
  roles:
  - role: bootstrap
  - role: hurricanehrndz.rustup
    vars:
      rustup_user: "{{ ansible_user }}"
      rustup_cargo_crates: ["samply"]
      rustup_default_version: "nightly"