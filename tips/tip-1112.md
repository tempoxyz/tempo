---
id: TIP-1112
title: EOA Root-Key Disablement
description: Rejects bare primitive authority once an account has a committed configuration.
authors: Tanishk Goyal (@legion2002), Jake Moxey (@jxom)
status: Draft
related: TIP-0001, TIP-1020, TIP-1114, TIP-1107, TIP-1108, TIP-1111, TIP-1113
protocolVersion: T13
---

# TIP-1112: EOA Root-Key Disablement

## Abstract

This TIP disables an account's bare root authority once it stores a nonzero configuration hash. The account must use its current owner configuration or an explicitly authorised access key. The check covers every primitive-authority role in the protocol. The retired key may still contribute as an explicitly configured owner.

## Motivation

An old root key must not bypass the account's configured owners. Disabling it allows existing accounts to migrate safely to configurable ownership and is a prerequisite for future post-quantum account protection.

## Assumptions

[TIP-1108](./tip-1108.md) prevents a nonzero commitment from being cleared. This makes root disablement permanent without a separate flag or toggle.

# Specification

## Authority check

From T13, read the recovered primitive address's account-leaf commitment before accepting it as direct authority.

- If the commitment is zero, preserve existing primitive-signature behaviour.
- If it is nonzero, reject bare root authority with `RootKeyRetired(account)`.

Apply this check to every primitive-authority role:

- Direct transaction senders.
- Signers of key authorisations.
- Fee payers.
- Access-key identities.

For an access key, check the key ID supplying the signature. Do not apply this check to the parent account whose funds it accesses. A configurable parent may use active delegates under TIP-1111.

Check EIP-7702 and Tempo authorisation-list authorities before changing delegation.

- Skip an ineligible entry where the existing authorisation-list rules require skipping.
- That entry must never install or remove delegation for the retired account.
- Existing skipped keychain entries remain skipped.

## Protocol precompiles

From T13, every protocol precompile must apply the same check before treating a primitive signature or an implicit root-key match as account authority.

- TIP-20 `permit` checks the owner's commitment before granting an allowance.
- Channel vouchers check the commitment of the expected signer. This is the payer when no separate signer is set, otherwise the channel's authorised signer.
- `verifyKeychain` and `verifyKeychainAdmin` check the primitive signing key's commitment before accepting its authority.
- `isAdminKey` and internal admin checks must not treat `key_id == account` as implicit root authority when that account has a nonzero commitment.

Use the commitment at the time of the call, including changes made earlier in the same transaction.

- Operations that rely on retired root authority revert with `RootKeyRetired(account)` before changing state.
- Boolean authority checks return `false` for retired root authority.
- These rules also apply to signatures created before the root was disabled and submitted later by a relayer.

Calls made by an account through an authenticated transaction still use its `msg.sender` authority. A nonzero commitment does not block those calls.

Pure signature checks remain unchanged, including `ecrecover` and TIP-1020 `recover` and `verify`. They prove who signed, not whether that signer currently controls an account. A protocol precompile using their result as authority must perform the commitment check itself.

This TIP does not add multisig signature formats to permits or vouchers. The explicit owner-signature exception below still applies.

## Explicit owner signatures

A retired root key may still be explicitly listed as an owner.

- It signs inside a valid multisig witness.
- Its signature follows TIP-1114 and contributes only its configured weight.

Do not apply the bare-root check to these owner approvals. They prove an owner's approval, rather than granting the key independent authority over the account.

A configurable account can pay its own normal transaction fees. It cannot sign as a separate primitive fee payer. This TIP does not add multisig fee-payer signatures.

## State ordering and permanence

Use the commitment at the transaction's position in the block.

- Migration authenticates before storing the commitment. Later transactions cannot reuse the old root authority.
- Pending transactions must be revalidated after commitment changes or reorgs. A cached zero is not proof of current authority.

TIP-1114 stores the initial commitment before the first transaction's calls execute.

- From T13, root-key disablement applies during those calls and remains in effect after transaction success.
- Accounts registered in T12 become subject to root-key disablement when T13 activates.
- An address that has only received funds is not yet registered. Its zero commitment does not disable root authority.

Existing access-key grants remain subject to TIP-1111. Root disablement does not revoke them.

## Gas accounting

Reads follow the normal account-access schedule.

- An already loaded commitment adds no separate read or surcharge.
- This TIP writes no state.

# Tooling

- Root-key disablement is guaranteed only at the protocol level; libraries, wallets, and dapps may not recognise it.
- Permit2 and other dapps using `ecrecover` need a separate disablement check before accepting account authority; otherwise they may accept signatures from retired keys.
- Distinguish retired-root errors from invalid signatures.
- A zero commitment does not by itself identify an ordinary EOA.

# Observability

- This TIP introduces no new events.
- Initial registration is visible in included transactions and account state, even if execution fails.
- Owner updates and migration have their own events.
- Validation errors should identify the retired account and authority role.

# Threat Model

The old root key may remain known or become compromised. Disablement must cover every protocol role through which that key could act independently.

It does not change offchain signatures or recovery on other chains.

# Invariants

- After its first included transaction, a configurable account has a nonzero commitment, even if execution fails.
- From T13, an account with a nonzero commitment has no bare root authority.
- A retired root may still contribute its configured weight as an explicit owner.
- Access-key authority checks apply to the signing delegate's key ID.
- A configurable parent's nonzero hash does not itself reject an active delegate.
- Later transactions in the same block cannot reuse retired root authority in any protocol role.
- Skipped authorisation-list entries cannot install or remove delegation for the retired account.
- Permits, channel vouchers, and keychain authority checks use the configuration hash at call time, including changes earlier in the transaction.
- Relaying a signature created before disablement does not restore its authority.
