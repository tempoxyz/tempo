---
id: TIP-1044
title: Waive storage initialization cost for DEX flip order placement
description: Eliminates the 250,000-gas state creation cost when a flip order creates a new order, since the filled order's storage is cleared in the same call.
authors: Dan Robinson (@danrobinson)
status: Draft
related: TIP-1000, TIP-1002
---

# TIP-1044: Waive storage initialization cost for DEX flip order placement

## Abstract

When a StablecoinDEX flip order is filled, the precompile deletes the old order's storage slots and writes a new order to fresh slots. Under TIP-1000's gas schedule the new order pays the full 250,000-gas state creation cost per slot, even though an equal number of slots are simultaneously cleared. This TIP introduces a precompile-level gas discount that waives the state creation cost for the new order created during a flip, making flip order execution significantly cheaper for swappers.

## Motivation

Flip orders are designed as perpetual two-sided liquidity: when one side fills, a new order is automatically placed on the opposite side. Because the old order is deleted and the new order is created in the same `fill_order` call, the net state growth is zero — the same number of storage slots are cleared as are initialized.

However, the SSTORE gas schedule prices each slot independently. Per TIP-1000, Tempo charges 250,000 gas for writing a new state element (zero → non-zero), while clearing a slot (non-zero → zero) earns only a 15,000 gas refund capped at 20% of total transaction gas by EIP-3529. The refund is negligible compared to the initialization cost, so swappers pay the full state creation price even though no net storage is consumed.

This makes filling flip orders substantially more expensive than filling regular orders of the same size, discouraging their use and increasing costs for swappers who interact with them. Since the StablecoinDEX is a precompile with full control over its own gas accounting, it can apply a targeted discount without modifying the general EVM gas schedule.

## Assumptions

- The `Order` struct's storage footprint does not change between the filled order and the newly placed flip order. Both use the same number of storage slots (determined by the `Storable` derive macro).
- The old order is always fully deleted (`orders[old_id].delete()`) in the same call that places the new order (`place_flip()`). This is guaranteed by the `fill_order` control flow.
- This discount applies only within the StablecoinDEX precompile's `fill_order` path and does not affect general SSTORE gas accounting elsewhere in the EVM.
- If the `Order` struct layout changes in a future TIP (adding or removing fields), the discount must be re-evaluated to ensure slot counts still match.

---

# Specification

## Gas discount mechanism

When `fill_order` processes a flip order (i.e., `order.is_flip() == true`), the precompile applies a gas discount equal to the SSTORE initialization cost that would be charged for the new order's storage slots.

Specifically:

1. **Identify the slot count**: Determine the number of storage slots occupied by an `Order` struct (currently derived from the `Storable` macro based on field sizes).
2. **Calculate the discount**: `discount = slot_count × STATE_CREATION_COST` where `STATE_CREATION_COST = 250,000` (per TIP-1000).
3. **Apply the discount**: After `place_flip()` and `orders[old_id].delete()` complete, refund `discount` gas via `self.storage.refund_gas(discount)`.

The discount is applied as a gas refund within the precompile's storage accounting layer, bypassing the EIP-3529 refund cap since it is internal to the precompile and not part of the transaction-level refund mechanism.

## Scope

- **Applies to**: The `fill_order` method in `StablecoinDEX` when processing a fully filled flip order that successfully places a new order via `place_flip()`.
- **Does not apply to**: Initial `placeFlip()` calls, regular order placement, order cancellation, or any non-DEX storage operations.
- **Does not apply if flip fails**: If `place_flip()` returns a business logic error (e.g., insufficient balance, policy rejection), no discount is applied since no new order was created.

## Tick-level storage

The discount covers only the `Order` struct slots. Tick-level storage changes (clearing an exhausted tick level, initializing a new tick level) are **not** discounted, because:

1. The old and new orders may be at different ticks, so tick-level storage changes are not guaranteed to be symmetric.
2. Tick-level slots may already be warm from other orders at the same tick.

## Hardfork gating

This change is gated behind a hardfork flag. The discount is only applied when the active spec satisfies the gating check.

---

# Invariants

- **Net state neutrality**: A flip order fill always deletes exactly as many `Order` storage slots as it creates. The discount must never exceed the actual initialization cost incurred.
- **No discount on flip failure**: If `place_flip()` fails, no discount is applied.
- **Gas accounting correctness**: Total gas used by a transaction containing a flip fill must equal `(standard gas) - discount`. The discount must not cause gas used to underflow.
- **Isolated to precompile**: The discount mechanism must not affect SSTORE gas accounting for any contract or precompile outside of `StablecoinDEX.fill_order`.
