---
id: TIP-1036
title: T2 Hardfork Bug Fixes
description: Meta TIP collecting all audit-driven bug fixes and hardening changes gated behind the T2 hardfork.
authors: Tanishk (@legion2002), Rusowsky (@0xrusowsky), Fgimenez (@fgimenez), Decofe (@decofe), Howy (@howydev), Kitsune (@0xKitsune)
status: In Review
related: N/A
protocolVersion: T2
---

# TIP-1036: T2 Hardfork Bug Fixes

## Abstract

This is a meta TIP that collects all audit-driven bug fixes, security hardening, and minor correctness improvements gated behind the T2 hardfork activation. Each change was identified through the Cyclops audit engagement or internal review and is individually small, but together they form the complete set of state-function-breaking fixes shipping in T2.

## Motivation

During the Cyclops audit cycle (SIGP + in-house reviews), several issues were identified across precompiles, the transaction pool, fee handling, and the account keychain. Individually these fixes do not warrant standalone TIPs, but they must be documented together as they collectively define the T2 STF delta beyond feature TIPs.

---

# Changes

## 1. Require `tx.origin` for AccountKeychain admin ops

**PR**: [#3202](https://github.com/tempoxyz/tempo/pull/3202) · **Author**: @legion2002

T2 scopes `authorizeKey`, `revokeKey`, and `updateSpendingLimit` to require `msg_sender == tx_origin` and `transaction_key == Address::ZERO`. This prevents contract-mediated confused-deputy flows from reusing transaction-global root-key status to administer keys on behalf of the caller.

## 2. Reject self-sponsored fee payer signatures

**PR**: [#3200](https://github.com/tempoxyz/tempo/pull/3200) · **Author**: @legion2002

Rejects AA transactions where the `fee_payer_signature` resolves back to the sender, preventing self-sponsored signatures from bypassing fee-payer assumptions. Enforced in both txpool validation and EVM fee-payer resolution.

## 3. Run invalidation eviction on reorg segments

**PR**: [#3171](https://github.com/tempoxyz/tempo/pull/3171) · **Author**: @fgimenez

On chain reorgs, invalidation events (key revocations, spending limit changes, validator/user token changes, blacklist additions) in the new canonical segment were silently ignored because maintenance continued before reaching `TempoPoolUpdates::from_chain()`. The fix extracts updates from the reorg segment and runs eviction for both the main and paused pools.

## 4. Remove unnecessary SLOAD in packed field structs

**PR**: [#2976](https://github.com/tempoxyz/tempo/pull/2976) · **Author**: @0xrusowsky

Skips the redundant `SLOAD` in the precompile `store` macro for the first packed slot of a struct. Since struct fields occupy their own slots (never packed with unrelated fields), starting from `U256::ZERO` instead of reading the existing value saves 1 cold SLOAD per packed-struct write.

## 5. Add `AccessKeySpend` event

**PR**: [#2914](https://github.com/tempoxyz/tempo/pull/2914) · **Author**: @decofe

Emits `AccessKeySpend(account, publicKey, token, amount, remainingLimit)` from `verify_and_update_spending` whenever an access key's spending limit is deducted. Only fires for actual access-key spends — root key transactions return early before emission.

## 6. Check token paused in internal DEX balance swaps

**PR**: [#3204](https://github.com/tempoxyz/tempo/pull/3204) · **Author**: @0xrusowsky

Adds a `check_not_paused()` call in `StablecoinDEX` internal balance transfers gated behind `is_t2()`. Previously, swaps using internal DEX balances could bypass the token pause state.

## 7. Correct built-in policy type data for TIP403Registry

**PR**: [#3203](https://github.com/tempoxyz/tempo/pull/3203) · **Author**: @0xrusowsky

Built-in policies (`REJECT_ALL` / `ALLOW_ALL`) are virtual and not stored on-chain. On T2, `policyData()` now returns the correct `PolicyType` (`WHITELIST` / `BLACKLIST` respectively) and `Address::ZERO` admin for these built-in IDs instead of falling through to storage reads.

## 8. Guard `normalize_p256_s` against out-of-range values

**PR**: [#3101](https://github.com/tempoxyz/tempo/pull/3101) · **Author**: @decofe

Changes `normalize_p256_s` to return `Option<B256>`, rejecting `s == 0` and `s >= P256_ORDER` before subtraction. Without this guard, unsigned subtraction wraps in release mode and can produce an invalid scalar.

## 9. Resolve compound policy sub-policy IDs for eviction

**PR**: [#3196](https://github.com/tempoxyz/tempo/pull/3196) *(merged)* · **Author**: @fgimenez

Fixes txpool eviction to correctly resolve sub-policy IDs within compound policies, ensuring that blacklist additions propagate to transactions authorized through compound policy chains.

## 10. Strict payment calldata validation in pool and builder

**PR**: [#3099](https://github.com/tempoxyz/tempo/pull/3099) *(merged)* · **Author**: @decofe

Adds type-safe calldata matching for payment lane classification using `SolCall::SELECTOR` and `SolType::ENCODED_SIZE`, preventing transactions with unrecognized or oversized calldata from being classified as payments and accessing the payment lane gas budget.

## 11. Reject legacy invalid policy types in compound sub-policies

**PR**: [#3188](https://github.com/tempoxyz/tempo/pull/3188) *(merged)* · **Author**: @howydev

Uses `is_simple()` instead of `!is_compound()` to validate compound policy sub-policies, rejecting legacy type-255 policies that previously passed the negated check.

## 12. Built-in ecrecover and keccak support for precompiles

**PR**: [#3155](https://github.com/tempoxyz/tempo/pull/3155) *(merged)* · **Author**: @0xrusowsky

Adds built-in `keccak256()` and `ecrecover()` to `PrecompileStorageProvider` with automatic gas metering, so consuming precompiles no longer need to handle gas accounting for these primitives.

## 13. Handle T2 policy errors in DEX

**PR**: [#3015](https://github.com/tempoxyz/tempo/pull/3015) *(merged)* · **Author**: @0xrusowsky

Updates DEX precompiles to handle the new `TIP403RegistryError::InvalidPolicyType` error returned by `policy_type()` post-T2, replacing the old `Panic(UnderOverflow)` sentinel.

## 14. Gate DEX `place_flip` order under T2

**PR**: [#2711](https://github.com/tempoxyz/tempo/pull/2711) *(merged)* · **Author**: @0xrusowsky

Gates the `place_flip` order fix behind the T2 hardfork to preserve pre-T2 re-execution compatibility.

## 15. Return zero remaining limit for revoked keys

**PR**: [#2553](https://github.com/tempoxyz/tempo/pull/2553) *(merged)* · **Author**: @0xrusowsky

`getRemainingLimit()` now returns zero for revoked or non-existent access keys instead of a stale positive value. Ref: `SIGP-31`.

## 16. Nonce key gas repricing

**PR**: [#2533](https://github.com/tempoxyz/tempo/pull/2533) *(merged)* · **Author**: @0xrusowsky

Increases intrinsic gas costs for 2D nonce keys on T2 to account for extended storage lookups: existing keys add `2 × WARM_SLOAD`, new keys add `2 × WARM_SLOAD`. Ref: `SIGP-7`.

## 17. Error with `PolicyNotFound` for inexistent policy IDs

**PR**: [#2618](https://github.com/tempoxyz/tempo/pull/2618) *(merged)* · **Author**: @0xrusowsky

`get_policy_data()` now reverts with `PolicyNotFound` for non-existent policy IDs instead of silently returning default values. Ref: `SIGP-13`.

## 18. Refund spending limit for unused gas fees

**PR**: [#2528](https://github.com/tempoxyz/tempo/pull/2528) *(merged)* · **Author**: @legion2002

Restores access key spending limits by the refunded gas amount in `transfer_fee_post_tx()`. Previously the full max fee was permanently deducted from the spending limit regardless of actual gas used. Ref: `SIGP-8`.

## 19. Tick spacing checks on DEX price conversion functions

**PR**: [#2513](https://github.com/tempoxyz/tempo/pull/2513) *(merged)* · **Author**: @0xKitsune

Adds tick spacing validation to `tick_to_price` and `price_to_tick`, rejecting ticks that don't align with the pool's configured spacing.

## 20. Reserved liquidity transient storage check

**PR**: [#2496](https://github.com/tempoxyz/tempo/pull/2496) *(merged)* · **Author**: @0xKitsune

Adds a transient storage (`TSTORE`/`TLOAD`) guard to prevent reserved liquidity from being double-spent within the same transaction.

# Invariants

All existing protocol invariants are preserved. Each fix either tightens validation (rejecting previously-accepted invalid states) or corrects event emission / storage reads without introducing new state transitions. The key invariants reinforced:

1. **Admin-op isolation**: AccountKeychain admin operations can only be invoked by the account owner via a direct transaction (not via contract call).
2. **Fee-payer distinctness**: The fee payer of an AA transaction must be a different account than the sender.
3. **Reorg consistency**: Pool eviction runs on the new canonical segment after every reorg.
4. **Token pause enforcement**: Paused tokens cannot be moved through any path, including internal DEX balances.
5. **Policy correctness**: Built-in policy queries return semantically correct types; inexistent policy IDs revert with `PolicyNotFound`.
6. **Signature scalar validity**: P256 `s` values are range-checked before normalization.
7. **Spending limit accuracy**: Access key spending limits reflect actual gas consumed, not the max fee estimate.
8. **Revoked key consistency**: Revoked or non-existent access keys always report zero remaining limit.
9. **Nonce key gas correctness**: 2D nonce key gas accounts for the full storage lookup cost.
10. **Policy type strictness**: Legacy invalid policy types (type-255) are rejected in compound sub-policy validation.
11. **DEX price boundary safety**: Tick-to-price and price-to-tick conversions reject misaligned ticks.
12. **Liquidity atomicity**: Reserved liquidity cannot be consumed more than once within a transaction.
