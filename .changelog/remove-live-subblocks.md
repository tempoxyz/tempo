---
tempo-primitives: major
---

Removed the live `SubBlock`, `SignedSubBlock`, and `RecoveredSubBlock` APIs. Legacy subblock metadata and transaction decoding remain available for historical replay.
