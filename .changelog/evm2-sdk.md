---
"tempo-hardfork": major
---

Remove the published `evm` feature and its revm `SpecId` conversions as part of the EVM2 migration. EVM integration remains available in the repository but is excluded from the published SDK crates.
