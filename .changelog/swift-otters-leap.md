---
tempo-alloy: major
tempo-contracts: major
tempo-primitives: major
---

Regenerated SDK bindings against the latest precompile interfaces, renaming and removing some `sol!`-generated error/event/call variants and structs (e.g. `ITIP20::TransfersDisabled`, `ITIP20::StringTooLong`, `IFeeManager::OnlyValidator`, `ITIP20Factory::createTokenCall`) to match the current Solidity surface.
